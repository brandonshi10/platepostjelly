# Next.js / Vercel
# Use a local or PlatePost development deployment. Never a Production deploy
# key, and never copy production secrets here. Two ways to fill these in:
#   1. A local, account-free deployment: run
#      `CONVEX_AGENT_MODE=anonymous pnpm exec convex dev --once`, which
#      writes CONVEX_DEPLOYMENT/NEXT_PUBLIC_CONVEX_URL/
#      NEXT_PUBLIC_CONVEX_SITE_URL into the gitignored .env.local for you.
#   2. A real PlatePost development deployment: only after the connection
#      checklist in docs/PLATEPOST_INTEGRATION.md is complete, run
#      `pnpm exec convex dev --configure existing --dev-deployment local`
#      (see AGENTS.md rule 1 before ever pointing this repository's
#      `convex dev` at a shared PlatePost deployment).
# Never set CONVEX_DEPLOY_KEY here or in any script this repository runs;
# `convex deploy` must never be invoked from this project (see AGENTS.md).
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
CONVEX_URL=

# Public browser map token
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# PlatePost server authentication
# Generate independent, long random values. Set PLATEPOST_CONVEX_SERVICE_KEY
# to the same value in Vercel and the matching Convex deployment.
PLATEPOST_CONVEX_SERVICE_KEY=
JELLYHUNT_API_KEY=

# Signs opaque pagination cursors returned by owner history and leaderboard
# endpoints. Generate a separate long random value; do not reuse another key.
JELLYHUNT_CURSOR_SECRET=

# Jelly-issued native mission token verification (Next.js server only).
# Jelly creates the token endpoint/JWKS. Tokens are limited to five minutes,
# audience platepost-jellyhunt, and jellyhunt:read / jellyhunt:submit scopes.
JELLY_MISSION_JWKS_URL=
JELLY_MISSION_TOKEN_ISSUER=

# Local UI development only. Use "fixture" locally or "convex" for real data.
# Fixture mode is disabled automatically in production.
JELLYHUNT_DATA_SOURCE=convex

# PlatePost admin login and signed HTTP-only session
JELLYHUNT_ADMIN_USERNAME=
JELLYHUNT_ADMIN_PASSWORD=
JELLYHUNT_ADMIN_SESSION_SECRET=

# Optional public app-store overrides
NEXT_PUBLIC_JELLY_IOS_APP_URL=https://apps.apple.com/us/app/jellyjelly-human-social/id6505022038
NEXT_PUBLIC_JELLY_ANDROID_APP_URL=https://play.google.com/store/apps/details?id=app.jellyjelly.prod

# Convex environment variables
# Add these in the Convex dashboard; do not expose them to browser code.
JELLY_API_BASE_URL=https://api.jellyjelly.com
JELLY_API_TOKEN=
JELLYHUNT_MAX_REWARD_AMOUNT=10000

# Verification is scheduled after intake by default. Set false only for an
# intentional operational pause; it does not enable rewards.
JELLYHUNT_VERIFICATION_AUTORUN_ENABLED=true

# Preferred production partner integration
# Credential-bearing non-loopback endpoints must use HTTPS.
# The base URL is used by Convex verification and reward workers. The exact
# preflight URL is used by the Next.js submission route. JELLY_API_BASE_URL
# is also used for the place-linked Jelly feed.
JELLY_PARTNER_API_BASE_URL=
JELLY_PARTNER_PREFLIGHT_URL=
# Transitional exact-URL adapter settings; leave blank for the namespaced
# partner-base workflow unless the v1 adapter is intentionally in use.
JELLY_PARTNER_VERIFY_URL=
JELLY_PARTNER_REWARD_URL=
JELLY_PARTNER_API_KEY=
JELLY_PARTNER_TIMEOUT_MS=10000

# Reward worker safety gates. Keep automatic rewards false in every new
# environment. Identity must be exactly development, preview, or production;
# missing/misspelled values fail closed. Production additionally requires
# explicit approval. An uncertain attempt is never retried automatically.
JELLYHUNT_AUTOMATIC_REWARDS_ENABLED=false
JELLYHUNT_ENVIRONMENT_IDENTITY=development
JELLYHUNT_PRODUCTION_REWARDS_APPROVED=false

# Signed Jelly -> Convex reward webhooks. Use independently generated values;
# PREVIOUS exists only for a controlled key-rotation overlap window.
JELLYHUNT_WEBHOOK_SECRET_CURRENT=
JELLYHUNT_WEBHOOK_SECRET_PREVIOUS=

# Migration-only legacy integration
JELLY_LEGACY_API_TOKEN=
JELLY_REWARD_API_TOKEN=
JELLY_REWARD_AUTH_SCHEME=Token
# Backward-compatible fallback only; prefer JELLY_REWARD_API_TOKEN.
JELLY_REWARD_BEARER_TOKEN=
